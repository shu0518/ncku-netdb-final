{"schema_ddl": "CREATE TABLE Departments (did INTEGER PRIMARY KEY, name TEXT NOT NULL);\nCREATE TABLE Doctors (doc_id INTEGER PRIMARY KEY, name TEXT NOT NULL, did INTEGER NOT NULL);\nCREATE TABLE Patients (pid INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER NOT NULL);\nCREATE TABLE Appointments (aid INTEGER PRIMARY KEY, doc_id INTEGER NOT NULL, pid INTEGER NOT NULL, date TEXT NOT NULL, status TEXT NOT NULL);", "sql": "SELECT T1.name, T1.age FROM Patients AS T1 JOIN Appointments AS T2 ON T1.pid = T2.pid GROUP BY T1.pid ORDER BY T1.age DESC LIMIT 3;"}